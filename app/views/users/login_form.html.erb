<body>
<main>
    <h2>Log In</h2>

<%# <form action="/login" accept-charset="UTF-8" method="post"><input name="utf8" type="hidden" value="✓"><input type="hidden" name="authenticity_token"> %>
<%= form_with model: @user, url: login_path do |f| %>  
  <div class="form-group">
    <label for="name"><%= f.label :username %></label>
    <input class="form-control" <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <input class="btn btn-primary" <%= f.submit "Log In" %>
  </div>
<% end %>

</main>
</body>
